# 피드백


* 라이브러리 형태로 제공하는 것들은 C언어 인터페이스 형태로 해야 한다. (그렇게 하지 않으면 여러가지 복잡하 문제가 생긴다.)
(예를 들어, std::set<int>를 라이브러리에서 메인 프로그램으로 던지는것..)

이게 만일 DLL이라면 당연히 C언어형태의 ABI로.. 그런데 여기에서는 DLL없이 lib만 쓰고 있는거?? 다른 프로젝트에서 쓰지 않음이 확실하면 사용 가능.

참고로, 보통 서버는 x64로 빌드하고, 클라는 Win32로 빌드를 하는데 같은 lib를 쓰면 안되겠지?

* ISS가 Actor이어야 하는 이유가 있남?
사실상 인터랙션은 ISSModule하고 하는데?

* GameOption.h 파일 네이밍이 적절하지 않아 보임..
게임 내에서 쓰이는 상수 집합 같은데...

* 헐? 다른 프로젝트에 있는 헤더 파일을 인클루드하고 있네? 졸 위험함..
예: DDWrapper가 GameLogic의 environment.h 인클루드.


* 이 팀은 전체적으로 남의(?) 잘 만들어진 코드를 많이 읽어보는 훈련이 필요함.


고친 부분은 ///# 해놓은 부분 지울 것.
